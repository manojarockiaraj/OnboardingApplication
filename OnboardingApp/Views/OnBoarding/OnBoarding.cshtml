@using OnboardingApp.Models
@{
    ViewData["Title"] = "On Boarding Form";
}

@{
}

<div class="section-card">
    <div class="card-header">
        <h3 class="section-title"></h3>
    </div>
    <div class="card-content">
        @await Html.PartialAsync("_OnBoardingChecklist", new ViewDataDictionary(ViewData)
   {
       ["Candidate"] = ViewData["Candidate"],
       ["OnBoardingItems"] = ViewData["OnBoardingItems"]
   })
    </div>
</div>